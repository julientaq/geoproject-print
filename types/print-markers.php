<section class="marker">

<!-- get all the meta  -->
<h1>print the marker</h1>
<header>
    <h2><?php the_title() ?></h2>
</header>

<p>marker</p>
<?php 
// include 'reusable/map.php' 
?> 


<?php include 'reusable/showAllMeta.php' ?>


<?php 
// include 'reusable/tags.php' 
?>



</section>




<!-- usable tag per marker -->

<?php 
// gp_lat ⇒ latitude
// gp_lng ⇒ longitude
// gp_tiles_provider ⇒ osm (pour l’affichage sur le marqueur) ?
// gp_popup_text ⇒ rajout dans la popup: text
// gp_popup_image ⇒ rajout dans la popup: image
// gp_popup_video ⇒ rajout dans la popup: video (print?)
// gp_popup_audio ⇒ rajout dans la popup: audio (print?)
// gp_read_more_link ⇒ 
// gp_map ⇒ post de type map auquel le marqueur est rattaché 
// gp_icon_type ⇒ si icon n’est pas défaut
// gp_icon_filename ⇒ icone alternative
// gp_popup_content_order ⇒ ordre des contenus des popups ?
// display_image_une ⇒ no / si une image est utilisé en une plutôt que la carte. On va faire sans
// alaune_une ⇒ no  /si ça va à la une du site

?>